
# 🎯 Назначение каждой папки

## **ui/** - Базовые строительные блоки

- **Что хранить:** Простые компоненты без бизнес-логики
- **Примеры:** `Button`, `Input`, `Select`, `Checkbox`, `Radio`, `Textarea`, `DatePicker`, `Switch`, `Icon`, `Avatar`, `Badge`
- **Когда использовать:** Создаёте атомарный UI элемент, который будет использоваться в других компонентах

## **common/** - Переиспользуемые композитные компоненты

- **Что хранить:** Комбинации ui-компонентов для частого использования
- **Примеры:** `SearchInput`, `FilterGroup`, `Pagination`, `Breadcrumbs`, `Modal`, `Accordion`, `Tabs`, `FileUpload`
- **Когда использовать:** Нужен компонент из нескольких ui-элементов, который будет использоваться в разных частях приложения

## **modules/** - Сложные компоненты с бизнес-логикой

- **Что хранить:** Компоненты с внутренней структурой и логикой
- **Примеры:** `ProductCard`, `UserProfile`, `ShoppingCart`, `SearchBlock`, `Navigation`, `DataTable`, `ReviewList`, `OrderSummary`
- **Когда использовать:** Компонент состоит из нескольких других компонентов и содержит бизнес-логику или состояние

## **templates/** - Шаблоны целых страниц

- **Что хранить:** Готовые макеты страниц, комбинация модулей
- **Примеры:** `MainPage`, `CatalogPage`, `ProductPage`, `AuthPage`, `ProfilePage`, `CheckoutPage`, `BlogPage`
- **Когда использовать:** Создаёте шаблон целой страницы, компонуете несколько модулей в законченный макет

## **layout/** - Компоненты макета приложения

- **Что хранить:** Компоненты для общей вёрстки приложения
- **Примеры:** `Header`, `Footer`, `Sidebar`, `Layout`, `Navigation`, `Breadcrumbs`, `PageContainer`
- **Когда использовать:** Компонент отвечает за общий макет, содержит навигацию или общие элементы страниц

## 🔄 Правила использования

## Иерархия зависимостей

ui/ → common/ → modules/ → templates/ → pages/

**Можно:**

- `common/` использует `ui/`
- `modules/` использует `ui/` и `common/`
- `templates/` использует все предыдущие уровни
- `pages/` использует все компоненты

**Нельзя:**

- `ui/` использует `modules/` или `templates/`
- `common/` использует `modules/`
- Нарушать направление зависимостей "снизу вверх"

## 📝 Практические рекомендации

1. **Начинайте с `ui/`** - создавайте базовые элементы первыми
2. **Думайте о переиспользовании** - если компонент будет в 2+ местах, вероятно в `common/`
3. **Разделяйте логику** - бизнес-логику в `modules/`, презентацию в `ui/`
4. **Соблюдайте иерархию** - не нарушайте направление зависимостей

## 💡 Примеры использования

```typescript
// templates/ProductPage.tsx - использует всё
const ProductPage = () => {
  return (
    <Layout>                    {/* layout/ */}
      <ProductGallery />        {/* modules/ */}
      <ProductInfo />           {/* modules/ */}
      <AddToCartButton />       {/* common/ */}
      <Button>Купить</Button>   {/* ui/ */}
    </Layout>
  );
};
```
